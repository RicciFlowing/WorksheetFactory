require 'rails_helper'

RSpec.describe Factory, type: :model do
  # let(:skill) { mock_model('Skill')}
  # let(:factory) {Factory.new(name: 'test', skills: [skill])}
  #
  # describe 'questions' do
  #   it 'returns some questions' do
  #       expect(factory.questions).to_not be_empty?
  #   end
  # end
end
