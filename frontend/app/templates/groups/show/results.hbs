<h2> Ergebnisse für {{worksheetSet.title}} </h2>

<form>

<div class="row">


  <div class="col-md-9">
    <ul class="list-group">
      {{#each worksheet.questions as |quest index|}}
      <li class="list-group-item flex-container space-between">
        {{number-clicker  number=0 class="green"}}
        <div class="margin-side">
          <h4> {{index}}. Frage </h4>
          <p>
             {{k-tex quest.text}}
          </p>
        </div>
        {{number-clicker  number=0 class="red"}}
      </li>

      {{/each}}
    </ul>
  </div>

  <div class="col-md-3">
    <ul class="list-group">
      {{#each worksheetSet.worksheets as |sheet|}}
           {{link-to sheet.title 'groups.show.results' worksheetSet.id sheet.id class="list-group-item"}}
      {{/each}}
    </ul>
  </div>
</div>



<!--
<table class="table">
  <thead>
    <th>
      Arbeitsblatt
    </th>
    {{#each numbers as |number|}}
    <th>
      {{number}}
    </th>
    {{/each}}
  </thead>


  {{#each worksheetSet.worksheets as |worksheet|}}
  <tr>
    <td>
    {{worksheet.title}}
    </td>
    {{#each worksheet.questions as |question index|}}
    <td>
      {{bs-form-element controlType="number" label="Richtig" inline="true" property=""}}
      {{bs-form-element controlType="number" label="Falsch"  inline="true" property=""}}
    </td>
    {{/each}}
  </tr>
  {{/each}}

</table> -->

<div class="form-group">
  <button type="button" {{action 'submit'}} class='btn btn-default'> Ergebnisse erfassen </button>
</div>
</form>
