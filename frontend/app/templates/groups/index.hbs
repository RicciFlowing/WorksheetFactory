<div class="row">
  {{#each sortedGroups as |group|}}
  <div class="col-md-2">
         {{link-to group.fullName 'groups.show' group.id}}
  </div>
  {{/each}}
  <div class="col-md-2">
      {{link-to 'Neue Lerngruppe' 'groups.new'}}
  </div>

</div>
