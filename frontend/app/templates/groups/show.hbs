<h2> {{group.grade}} {{group.name}} {{group.year}}</h2>
<ul class="tab-list">
  {{#each group.worksheetSets as |set|}}
  <li class="tab-link">{{link-to set.title 'groups.show.worksheet_set' set.id}} </li>
  {{/each}}
  <li class="tab-link"> {{link-to 'Erstelle ein neues Set' 'groups.show.create_set'}} </li>
</ul>

<div class="tab-content">
{{outlet}}
</div>
