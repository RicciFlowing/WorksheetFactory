<h2> {{group.name}}  {{group.year}}</h2>
<div {{action 'delete'}} class="btn btn-danger pull-right">
  <span class="fa fa-cross"> </span> Delete
</div>
<div class="group-details">
  Grade: {{group.grade}}
</div>
<p>
  {{group.description}}
</p>

{{yield}}
